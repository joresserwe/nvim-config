return {
  "joryeugene/dadbod-grip.nvim",
  -- Don't use version = "*": the author reset tags from v3.x back to v1.x,
  -- so semver "*" pins to the pre-fix v3.3.1 and throws a lazy.lua spec error.
  opts = {},
}
